
angular.module(_APP_).config(function($provide) {
//  $provide.decorator('$exceptionHandler', ['$log', '$delegate', '$injector', 'stackTraceService',
//    function($log, $delegate, $injector, stackTraceService) {
//      return function(exception, cause) {
//        var
//
//          errorObj = {
//            message: exception.toString(),
//            stackTrace: stackTraceService.printStack({e: exception})
//          };
//
//        // injector is needed here to avoid circular dependencies issue
//        $injector.get('errorLoggingService').logError(errorObj);
//
//        $delegate(exception, cause);
//      };
//    }
//  ]);
});

